.venv/
__pycache__/
*.pyc
.env

# Session scratch (SDD ledgers, agent reports)
.superpowers/

# Runtime directories (default AICOM_WORKSPACE_ROOT / AICOM_ARTIFACT_REPO_PATH):
# per-run agent workspaces and the artifact repo (its own nested git repo).
/workspaces/
/artifacts/
